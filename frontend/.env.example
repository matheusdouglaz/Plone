# Copie para `.env` na raiz do frontend e reinicie o Volto.
# O bloco de notícias RSS só chama GET {apiPath}/++api++/@observatorio-rss-feed — apiPath vem daqui.
RAZZLE_API_PATH=http://localhost:8080/Plone
